"use strict";
/**
 * core module
 * @module nCore/core
 */
var nCore = nCore || {};
nCore.core = (function(){
  /**
   * @function init
   * @description Выполняется при загрузке модуля
   */
  function init(){
    // console.log( this );
  };

  return {
    init: init
  };
})();